using UnityEngine;

public class PlayerWaterCheck : MonoBehaviour
{
    //public CandleFlame candle; // Ссылка на объект свечи
    //private KeyCode actionKey = KeyCode.E; // Клавиша для действия
    //void OnTriggerEnter(Collider other)
    //{
    //    if (other.CompareTag("Water") && !Input.GetKey(actionKey))
    //    {
    //        candle.Extinguish(); // Вызываем метод для погашения свечи
    //    }
    //}
}
